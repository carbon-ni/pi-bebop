---
task: TASK-0201
status: completed
verified: 2026-09-09
---

# TASK-0201

Implemented explicit Crew Session capture and single-Member addition.

- Added strict versioned Crew Session record domain and private atomic store.
- Added `session.capture` JSON-RPC schema, registry mapping, dispatch handler, and SessionManager metadata evidence.
- Added `crew session capture` and `crew session add` CLI leaves with deterministic structured output.
- Added manifest/Member/session validation, bounded concurrent capture, partial results, collision and drift handling.
- Pinned Pi host dev dependencies to 0.84.2 and added a compatibility test.

Verification: `npm test` (1284 passed), `npm run typecheck`, `npm run typecheck:presence-tests`, `npm run format:check`, and `npm run verify:package` passed. Funzzy watcher was unavailable because `.pi/funzzy.sock` did not exist.
